server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/alexanderjr-ui/Microservices-tutorial
          search-paths: config-data
  application:
    name: config-service